package dk.mathmagicians.playground.architecture.discovery.fixture.application;

/// The application's own plumbing without a stereotype: what an adapter depends on without a card to point at.
public record Defaults(String region) {
}
