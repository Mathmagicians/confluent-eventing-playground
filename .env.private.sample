# Confluent credentials for one environment. Copy to .env.test.private or .env.prod.private, both git-ignored.
# make sources the file for ENV (default test) into the command it runs.
KAFKA_BOOTSTRAP_SERVERS=
KAFKA_API_KEY=
KAFKA_API_SECRET=
SCHEMA_REGISTRY_REST_ENDPOINT=
SCHEMA_REGISTRY_ID=
SCHEMA_REGISTRY_API_KEY=
SCHEMA_REGISTRY_API_SECRET=
