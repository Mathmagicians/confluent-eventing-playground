/// The wire both Kafka adapters share: the topic per payload type and the header names of the envelope. Not a
/// module of its own; the publisher and the consumer are, in the packages below.
package dk.mathmagicians.playground.confluent.eventing.adapter.kafka;
